name: Deploy to AWS EC2

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build -t wadoodbangash/cloudtask-app:latest -f backend/Dockerfile .

      - name: Push to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "wadoodbangash" --password-stdin
          docker push wadoodbangash/cloudtask-app:latest

      - name: Deploy on EC2
        run: |
          docker pull wadoodbangash/cloudtask-app:latest
          docker stop cloudtask-app-prod || true
          docker rm cloudtask-app-prod || true
          docker run -d \
            --name cloudtask-app-prod \
            -p 8080:8080 \
            -e SPRING_MAIN_ALLOW_CIRCULAR_REFERENCES=true \
            -e SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/cloudtaskdb \
            -e SPRING_DATASOURCE_USERNAME=cloudtask_user \
            -e SPRING_DATASOURCE_PASSWORD=${{ secrets.DB_PASSWORD }} \
            -e JWT_SECRET=${{ secrets.JWT_SECRET }} \
            --restart unless-stopped \
            wadoodbangash/cloudtask-app:latest

      - name: Verify
        run: |
          docker ps | grep cloudtask-app-prod
          echo "✅ Deployed!"
